// Highlighting for code blocks
import 'prismjs/themes/prism.css';
// Global layout styles — imported here (single place, fixed order) so that
// mini-css-extract-plugin sees a consistent CSS order across all pages
import './src/layout/style.scss';
